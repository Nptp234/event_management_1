import 'dart:ui';

class StatisticModel{
  Color color;
  double value;
  String title, userState;
  StatisticModel({required this.color, required this.value, required this.title, required this.userState});
}